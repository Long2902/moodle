define([], function(){
 const init=()=>{document.querySelector('[data-action="new-item"]')?.addEventListener('click',()=>document.querySelector('[data-region="new-item-form"]')?.classList.toggle('d-none'));document.querySelectorAll('.wslib-row').forEach(row=>row.addEventListener('click',()=>{document.querySelectorAll('.wslib-row').forEach(x=>x.classList.remove('is-selected'));row.classList.add('is-selected');const b=document.querySelector('[data-region="detail-body"]'),e=document.querySelector('[data-region="detail-empty"]');if(e)e.classList.add('d-none');if(b)b.innerHTML=`<div class="wslib-preview-card"><span class="badge bg-primary">${row.dataset.kind.toUpperCase()}</span><h4>${row.dataset.name}</h4><p>Phiên bản hiện tại: ${row.dataset.version||'Chưa xuất bản'}</p><div class="wslib-detail-actions"><a class="btn btn-outline-primary btn-sm" href="detail.php?itemid=${row.dataset.item}">Mở chi tiết</a></div></div>`;}));};return {init};
});
