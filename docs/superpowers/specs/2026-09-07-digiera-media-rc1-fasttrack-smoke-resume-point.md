# DIGIERA Media System — RC1 Fast-Track Smoke Resume Point

Date: 2026-09-07
Target: Moodle 5.1 / Web01 + Web02
Status: RC1 Preview artifact independently verified and smoke-ready; deployment to Web01/Web02 is the next controlled action.

## 1. Source of truth

- Repository: `Long2902/moodle`
- RC1 branch: `feature/digiera-media-v1-rc1-fasttrack`
- Product-green commit: `63a4f025b72cb0e77bc217466e8310f6fcee6c9f`
- Self-contained packaging commit: `6134eabe8fe28a7245beef995fa380161cec93b4`
- Net diff from product-green to packaging commit: operational files only (`.digiera/rc1/INSTALL_AND_SMOKE.md`, `.github/workflows/digiera-media-rc1.yml`). No product PHP/JS/plugin logic changed.

Do not deploy from `main`, the old stage branch, or an earlier RC1 artifact. Use only the artifact produced from the self-contained packaging commit above after its RC1 release gate is green.

## 2. RC1 Preview scope now implemented

The intended smoke path is:

`TinyMCE → Học liệu DIGIERA → Library → select READY Media → create Reference → insert non-editable component → normal Moodle Save → central filter render → reopen/edit/save`

Implemented for this Preview RC:

- `local_digieramedia` schema/repositories/backup-restore foundation already present from the stage baseline.
- AJAX Media library search endpoint.
- AJAX draft Reference creation endpoint.
- Visibility contract supports owner, `GLOBAL`, `SHARED`, and matching `COURSE` items; the `SHARED` fix is required for the staging seed flow.
- `tiny_digieramedia` plugin with toolbar/menu entry.
- Modal A three-column library UI with Library / Recent / Trash / Upload lane presentation.
- TinyMCE marker round-trip hooks (`BeforeSetContent` and `PostProcess`).
- Production AMD build generated by Moodle Grunt.
- Central filter renderer for PDF, video, image, audio, and generic file links.
- PDF renderer uses `#zoom=page-width&scrollMode=page&spread=none`.
- Scoped CSS only; no intentional global RemUI navigation/tab selectors.
- RC1 artifact packaging with portable TGZ checksum, archive inventory, bundled seed helper, build provenance, and smoke guide.

## 3. Verification evidence

Release workflow: `DIGIERA Media RC1 fast-track`.

Expected gate sequence:

1. PHP/JS/RC1 contract syntax gate.
2. Moodle TinyMCE AMD build.
3. Moodle PHPUnit environment setup.
4. DIGIERA Media runtime suite.
5. Deterministic RC1 TGZ build.
6. Archive/checksum verification.
7. Artifact upload.

Runtime suite covers the existing schema, backup manifest, reference remapper, independent copy, course restore integration, and filter tests. The RC1 static contract additionally verifies the Tiny modal, marker round-trip, external services, renderer types, scoped CSS, Moodle Tiny option naming, and `SHARED` visibility contract.

Final workflow run: `34092280998` — SUCCESS for every RC1 job step.
Final artifact ID: `10007301834`.
Final artifact ZIP SHA256 / GitHub artifact digest: `7247b82a722d231b0c455a50a8745d5729e264dadd7f9c6bc89e0eecb62b66ee`.
Deterministic plugin TGZ SHA256: `64925842c37833029399d5a795bfdf773561a635b9d359f325f253b839d0d152`.

Independent post-download verification also passed outside GitHub Actions: ZIP integrity, ZIP SHA, portable TGZ `sha256sum -c`, required plugin/AMD entries, seed helper presence and PHP syntax, `SHARED` visibility spot-check, PDF page-width renderer spot-check, RC1 release strings, and absence of all temporary staging files.

## 4. Self-contained artifact contract

The GitHub Actions artifact must contain:

- `DIGIERA_MEDIA_MOODLE51_RC1_FASTTRACK.tgz`
- `DIGIERA_MEDIA_MOODLE51_RC1_FASTTRACK.sha256`
- `DIGIERA_MEDIA_MOODLE51_RC1_FASTTRACK.contents.txt`
- `INSTALL_AND_SMOKE.md`
- `digiera-stage-seed-pdf.php`
- `BUILD_INFO.txt`

The TGZ itself contains only these Moodle plugin trees:

- `local/digieramedia/`
- `filter/digieramedia/`
- `lib/editor/tiny/plugins/digieramedia/`

No R2 secret or production credential is packaged.

## 5. Controlled Web01/Web02 deployment target

Deployment is intentionally not claimed complete yet. The next controlled operation is:

1. Verify artifact ZIP and TGZ checksums.
2. Snapshot/preserve the existing three plugin trees on both Web01 and Web02.
3. Stop the Web01 cron timer and ensure the current cron service is no longer running.
4. Enable Moodle maintenance mode.
5. Install identical plugin trees on both nodes.
6. Compute and compare deterministic code-set hashes between Web01 and Web02.
7. Run `admin/cli/upgrade.php --non-interactive` exactly once on Web01 as the Moodle web user.
8. Set `cdnbaseurl=https://cdn.digiera.vn` and `pdfviewerurl=https://cdn.digiera.vn/pdfjs/web/viewer.html`; enable `filter_digieramedia` globally.
9. Purge Moodle caches and reload PHP-FPM on both nodes.
10. Disable maintenance and restart the Web01 cron timer.

## 6. First smoke path

Use the bundled `digiera-stage-seed-pdf.php` on Web01 with one disposable/test PDF already hosted at `https://cdn.digiera.vn/...pdf`.

Expected seed output includes `STAGE_SEED=PASS`, a Media UUID, a Reference UUID, object key, and diagnostic marker.

Then as an authorized Teacher/Admin:

1. Edit a Moodle Page using TinyMCE.
2. Confirm **Học liệu DIGIERA** is present.
3. Open Modal A and confirm three-column layout without RemUI navigation/header regression.
4. Confirm the seeded READY PDF is visible in Library (`SHARED` visibility).
5. Select it and press **Chèn vào bài**.
6. Confirm a non-editable DIGIERA component is inserted.
7. Save normally through Moodle.
8. Confirm the saved Page renders through DIGIERA PDF.js at page-width.
9. Reopen the Page editor and save again; the same logical Reference must survive the editor round-trip.
10. Repeat through Web01/Web02/load-balanced traffic.

## 7. Preview limitations — do not overclaim

This RC1 Preview does **not** certify final V1.

Still pending after successful smoke:

- live browser → R2 upload verification and UX completion;
- strict DRAFT → ACTIVE/STALE reference reconciliation;
- essential lifecycle management UI;
- migration canary and batch migration flow;
- remaining Backup/Restore/Course Publisher matrix and production qualification;
- broader operational/observability hardening.

DRAFT References are intentionally renderable in this Preview so a newly inserted Reference does not break before strict reconciliation is finished.

## 8. Next action

Do not merge to `main` yet. Keep the RC1 branch isolated until Web01/Web02 smoke evidence is captured.

After smoke:

- If any step fails: record exact node, URL/course/page, user role, browser console/network symptom, PHP/Moodle log excerpt, and the RC1 `BUILD_INFO.txt` values; fix on the RC1 branch and reissue one batch artifact.
- If the full smoke path passes on both nodes: snapshot the evidence in a new versioned Spec Kit resume point, then proceed to the next RC batch (live R2 upload + strict reference reconciliation first).
